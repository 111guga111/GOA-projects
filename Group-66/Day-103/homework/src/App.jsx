

const App = () => {
    `
        Working Directory: where you edit files.
        Staging Area: where you prepare files for commit (git add).
        Repository: where commits (project history) are stored.
        
    `
}

export default App;
